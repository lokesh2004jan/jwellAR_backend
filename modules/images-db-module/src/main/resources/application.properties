# App name
spring.application.name=ImagesDB

# Server port
server.port=8089
server.address=0.0.0.0

# AWS RDS MySQL Connection
spring.datasource.url=jdbc:mysql://database-1.cmvuw2qy2lij.us-east-1.rds.amazonaws.com:3306/jewellery_db?useSSL=true&requireSSL=true&verifyServerCertificate=false
spring.datasource.username=admin
spring.datasource.password=LokeshDawkar
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Hikari
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.validation-timeout=5000

# Initialize app-specific tables (watchlist/cart/addresses/orders)
spring.sql.init.mode=always
spring.sql.init.schema-locations=classpath:sql/create_app_tables.sql
